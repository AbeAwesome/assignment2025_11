For CountInSorted, I know the algorithm is O(logn) as I had tested it with extremely large inputs like 2^30 and it still took
within the upper bound of comparisons which is 2*log_2(n)+1. So, even though it doesn't pass the test case, I know it should be
logarithmic time complexity still.